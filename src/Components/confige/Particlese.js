import React from 'react';
import Particles from 'react-tsparticles';
import ParticlesConfig from './ParticlesConfig';

const Particlese = () => {
    return (
        <Particles params={ParticlesConfig}></Particles>
    );
};

export default Particlese;